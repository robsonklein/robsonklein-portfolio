{
  "name": "npm_sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-css": "sass content/scss/style.scss content/css/style.css",
    "watch-css": "sass --watch content/scss/style.scss content/css/style.css",
    "build": "npm run build-css && eleventy",
    "dev": "eleventy --serve",
    "start": "npm run build-css && npm run watch-css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robson Klein",
  "license": "ISC",
  "dependencies": {
    "jquery-toast-plugin": "^1.3.2"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sass": "^1.71.1"
  }
}
